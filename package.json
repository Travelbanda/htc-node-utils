{
  "name": "htc-node-utils",
  "version": "1.0.0",
  "description": "utils for node from htc",
  "main": "index.js",
  "author": "d.koltsov <d.koltsov@hotelchat.me>",
  "license": "MIT",
  "engines": {
    "node": "^8.6.0",
    "yarn": "^1.1.0"
  },
  "scripts": {
    "build": "tsc -d -p src --outDir dist",
    "compile": "tsc --noEmit -p .",
    "lint": "tslint -p src && tslint -p test && tslint webpack.config.js",
    "lint-fix": "tslint --fix -p src -t verbose",
    "prepublishOnly": "yarn build"
  },
  "repository": {
    "type": "git",
    "url": "git+ssh://git@github.com:Travelbanda/htc-node-utils.git"
  },
  "files": [
    "dist"
  ],
  "dependencies": {
    "@types/koa": "^2.0.46",
    "@types/koa-router": "^7.0.31",
    "@types/mongodb": "^3.1.3",
    "@types/node": "8.9.5",
    "koa": "^2.5.2",
    "koa-body": "^4.0.4",
    "koa-router": "^7.4.0",
    "mongodb": "^3.1.1",
    "raven": "^2.6.3",
    "typescript": "^3.0.1"
  }
}
